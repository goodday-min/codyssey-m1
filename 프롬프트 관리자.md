# 📝 나만의 프롬프트 관리 프로그램 (Prompt Manager)

터미널에서 메뉴 번호를 입력해 프롬프트를 추가·조회·검색·즐겨찾기 관리할 수 있는
파이썬 콘솔 프로그램입니다. 자주 쓰는 AI 프롬프트를 카테고리별로 정리하고,
필요할 때 빠르게 찾아볼 수 있도록 만들었습니다.

이 문서는 **Python/Git을 처음 다뤄보는 사람도 그대로 따라 하면
개발 환경 설정 → GitHub 저장소 생성 → 프로그램 실행 → 커밋/브랜치 실습**까지
끝낼 수 있도록 순서대로 정리했습니다.

---

## 목차

1. [프로그램 소개](#프로그램-소개)
2. [Step 1. 개발 환경 준비 (VSCode / Python / Git)](#step-1-개발-환경-준비-vscode--python--git)
3. [Step 2. GitHub 저장소 만들고 로컬과 연결하기](#step-2-github-저장소-만들고-로컬과-연결하기)
4. [Step 3. 프로그램 실행 방법](#step-3-프로그램-실행-방법)
5. [기능 목록](#기능-목록)
6. [데이터 저장 정책 (중요)](#데이터-저장-정책-중요)
7. [등록된 프롬프트 카테고리](#등록된-프롬프트-카테고리)
8. [코드 구조](#코드-구조)
9. [Step 4. 커밋 & 브랜치 실습 순서 (10개 이상 커밋 + 브랜치 병합)](#step-4-커밋--브랜치-실습-순서-10개-이상-커밋--브랜치-병합)
10. [Git 명령어 한눈에 정리](#git-명령어-한눈에-정리)
11. [학습 목표 자가 점검](#학습-목표-자가-점검)
12. [제출물 체크리스트](#제출물-체크리스트)

---

## 프로그램 소개

- 터미널(콘솔)에서 번호를 입력해 기능을 선택하는 메뉴 기반 프로그램입니다.
- 실행하는 동안 추가한 프롬프트/즐겨찾기 상태는 유지되며, 종료하면 초기화됩니다.  
  (단, 처음 등록하는 기본 프롬프트 3개는 예외입니다 — 아래 [데이터 저장 정책](#데이터-저장-정책-중요) 참고)

---

## Step 1. 개발 환경 준비 (VSCode / Python / Git)

### 1-1. VSCode 설치 및 확장 설치

1. [VSCode 공식 사이트](https://code.visualstudio.com/)에서 설치합니다.
2. VSCode를 열고 왼쪽 사이드바의 **확장(Extensions)** 아이콘(사각형 4개 모양) 클릭 →
   `Python` 검색 → **Python 확장(Microsoft 제공)** 설치.
3. (선택) 한글 메뉴를 원한다면 `Korean Language Pack` 검색 후 설치.
   설치 후 우측 하단에 뜨는 `Restart` 버튼을 눌러 VSCode를 재시작하면 한글 메뉴로 바뀝니다.

### 1-2. Python 설치 확인

VSCode 상단 메뉴 **터미널(Terminal) → 새 터미널(New Terminal)** 로 터미널을 엽니다.


```bash
python --version
# 또는 (환경에 따라)
python3 --version
```
<img width="350" height="45" alt="image" src="https://github.com/user-attachments/assets/a1727bf7-5661-448c-87e3-991bb3416cfb" />

- *Python 3.10 이상**이 출력되어야 함.
- 낮은 버전이거나 설치가 안 되어 있다면
  [python.org](https://www.python.org/downloads/) 에서 최신 버전을 설치.


### 1-3. Git 설치 및 초기 설정

```bash
git --version
```

- Git이 설치되어 있지 않다면 [git-scm.com](https://git-scm.com/downloads) 에서 설치.

내 이름과 이메일을 Git에 등록합니다. (커밋할 때마다 "누가 작업했는지" 기록에 사용됩니다)

```bash
git config --global user.name "내 이름"
git config --global user.email "내이메일@example.com"
```

기본 브랜치 이름을 `main`으로 통일합니다. (요즘 GitHub 기본값과 동일하게 맞추는 설정)

```bash
git config --global init.defaultBranch main
```

설정이 잘 되었는지 확인합니다.

```bash
git config --global --list
```
<img width="514" height="89" alt="image" src="https://github.com/user-attachments/assets/3a0f38ec-00ad-4480-a64f-c56d344965ce" />

### 1-4. VSCode에서 GitHub 계정 연동

1. VSCode 왼쪽 하단의 **계정(사람 모양 아이콘)** 클릭 → **Sign in with GitHub** 선택.
2. 브라우저 창이 열리면 GitHub 로그인 후 권한을 승인합니다.
3. 다시 VSCode로 돌아와 좌측 하단에 GitHub 계정 아이콘/이름이 표시되면 연동 완료입니다.
   (소스 제어 탭에서 GitHub 관련 메뉴가 활성화되어 있으면 정상 연동된 것입니다)

---

## Step 2. GitHub 저장소 만들고 로컬과 연결하기

### 2-1. GitHub에 새 저장소 생성

1. [github.com](https://github.com) 로그인 → 우측 상단 **+ → New repository**.
2. Repository name 입력 (예: `A1_1`).
3. Public/Private 선택 → **Create repository** 클릭.
   (README, .gitignore는 로컬에서 직접 만들 것이므로 자동 생성 체크는 하지 않아도 됩니다)
4. 생성된 저장소의 **HTTPS URL**을 복사해 둠.
   예: `https://github.com/goodday-min/A1_1.git`

### 2-2. GITHUB 저장소 clone 

```bash
git clone https://github.com/goodday-min/A1_1.git
ls 
git log --oneline
```
*clone 실행 예*
<img width="500" height="135" alt="image" src="https://github.com/user-attachments/assets/5e3fd581-9e4c-497c-b634-c84ad0626228" />

### 2-3. 프로젝트 폴더 생성 및 로컬 저장소 초기화

```bash
mkdir A1_1
git init
```

- `git init` : 현재 폴더를 Git이 관리하는 저장소로 만듬. (숨김 폴더 `.git` 생성)

### 2-4. 파일 준비

- 이 저장소에 있는 `A1_1.py`, `README.md`, `.gitignore` 파일을 `A1_1` 폴더 안에 복사해 넣는다.

### 2-5. 원격 저장소 연결 및 첫 커밋/푸시

```bash
git remote add origin https://github.com/A1_1/A1_1.git

git add .
git commit -m "chore: 프로젝트 초기화 및 .gitignore, README 추가"

git push -u origin main
```

- `git remote add origin <URL>` : 로컬 저장소와 GitHub 저장소를 연결합니다.
- `git add .` : 변경된 파일을 커밋 대상(스테이징 영역)에 올립니다.
- `git commit -m "메시지"` : 변경 내용을 하나의 기록(커밋)으로 저장합니다.
- `git push -u origin main` : 로컬 커밋을 GitHub의 `main` 브랜치로 업로드합니다.
  (`-u` 옵션은 이후부터 `git push`만 입력해도 되도록 연결을 기억시켜 줍니다)

  원격 저장소의 최신 내용을 내려받을 때는 `pull`을 사용합니다. 

```bash
git pull origin main
```

---

## Step 3. 프로그램 실행 방법

```bash
cd A1_1
python A1_1.py
```

- **최초 실행**이면 "기본 프롬프트 3개를 입력해주세요"라는 안내가 나옵니다.
  제목/내용/카테고리를 순서대로 3번 입력하면 `prompts.json` 파일에 저장됩니다.
- 이후 화면에 출력되는 메뉴에서 원하는 기능의 번호를 입력합니다.
- `0`을 입력하면 프로그램이 종료됩니다.

---

## 기능 목록

| 번호 | 기능 | 설명 |
|---|---|---|
| 1 | 프롬프트 추가 | 제목/내용/카테고리를 입력해 새 프롬프트를 등록합니다. 빈 값은 재입력을 요청합니다. |
| 2 | 프롬프트 목록 | 저장된 모든 프롬프트를 번호, 제목, 카테고리, 즐겨찾기 여부(⭐)와 함께 출력합니다. |
| 3 | 카테고리별 조회 | 카테고리를 선택하면 해당 카테고리의 프롬프트만 모아서 보여줍니다. |
| 4 | 프롬프트 검색 | 키워드를 입력하면 **제목 또는 내용**에 포함된 프롬프트를 검색합니다. |
| 5 | 프롬프트 상세 보기 | 번호를 입력하면 제목, 카테고리, 즐겨찾기 여부, 전체 내용을 보여줍니다. |
| 6 | 즐겨찾기 추가/해제 | 번호를 입력해 즐겨찾기 상태를 켜고 끌 수 있습니다. |
| 7 | 즐겨찾기 목록 | 즐겨찾기로 등록한 프롬프트만 모아서 보여줍니다. |
| 8 | 프롬프트 수정 | 기존 프롬프트의 제목/카테고리/내용을 수정합니다. |
| 9 | 프롬프트 삭제 | 확인 후 프롬프트를 목록에서 삭제합니다. |
| 0 | 종료 | 프로그램을 종료합니다. |

메뉴 번호가 아닌 값을 입력하면 안내 메시지를 출력하고 다시 메뉴로 돌아갑니다.

---

## 데이터 저장 정책 (중요)

| 구분 | 대상 | 저장 방식 | 프로그램 종료 시 |
|---|---|---|---|
| **기본 프롬프트** | 최초 실행 시 직접 입력한 3개 | `prompts.json`에 영구 저장 | **유지됨** (다음 실행에도 그대로 불러옴) |
| **임시 프롬프트** | 4번째부터 추가하는 프롬프트 | 메모리(리스트)에만 존재 | **초기화됨** (사라짐) |

- 처음 프로그램을 실행하면 `prompts.json` 파일이 없으므로, 기본 프롬프트 3개를 직접 입력받아 파일로 저장합니다.
  (이전 미션에서 작성한 프롬프트를 입력하면 됩니다)
- 기본 프롬프트 3개는 **수정·삭제도 가능**하며, 수정/삭제하면 `prompts.json`에도 바로 반영됩니다.
- 4번째 이후 추가한 프롬프트는 실행 중에는 자유롭게 추가·수정·삭제할 수 있지만 `prompts.json`에는 저장되지 않으므로,
  프로그램을 다시 실행하면 기본 3개만 남고 나머지는 초기화됩니다.
- `prompts.json`은 사람마다 입력 내용이 달라지는 개인 데이터 파일이라 `.gitignore`에 등록되어 있어 GitHub에는 올라가지 않습니다.

---

## 등록된 프롬프트 카테고리

프롬프트는 다음 6가지 카테고리 중 하나로 분류하거나, 직접 새 카테고리명을 입력할 수 있습니다.

- **텍스트 생성** – 블로그 글, 카피라이팅 등 텍스트 작성용 프롬프트
- **이미지 생성** – 이미지 생성 AI에 사용하는 프롬프트
- **영상 생성** – 영상 생성 AI에 사용하는 프롬프트
- **페르소나** – 특정 역할/전문가 캐릭터를 부여하는 프롬프트
- **자동화** – 코드 작성, 리뷰 등 업무 자동화용 프롬프트
- **기타** – 위 분류에 속하지 않는 프롬프트

---

## 코드 구조

한 함수에 모든 로직을 몰아넣지 않고, 기능별로 함수를 분리했습니다.

| 함수명 | 설명 |
| :--- | :--- |
| `show_menu()` | 메뉴 출력 |
| `input_prompt_details()` | 제목/내용/카테고리 입력 (공용 입력 로직) |
| `collect_initial_prompts()` | 최초 실행 시 기본 프롬프트 3개 입력받기 |
| `load_prompts()` / `save_default_prompts()` | 기본 프롬프트 JSON 불러오기/저장하기 |
| `add_prompt()` | 프롬프트 추가 |
| `show_all_prompts()` | 프롬프트 목록 |
| `view_by_category()` | 카테고리별 조회 |
| `search_prompt()` | 프롬프트 검색 (제목+내용) |
| `view_prompt_detail()` | 프롬프트 상세 보기 |
| `manage_favorite()` | 즐겨찾기 추가/해제 |
| `view_favorites()` | 즐겨찾기 목록 |
| `edit_prompt()` / `delete_prompt()` | 프롬프트 수정/삭제 |
| `main()` | 메뉴 입력을 받아 각 기능을 호출하는 메인 루프 |

---

## Step 4. 커밋 & 브랜치 (10개 이상 커밋 + 브랜치 병합)

> 각 커밋 전에는 `A1_1.py`에서 해당 기능 부분을 완성했다고 가정합니다.
> 이미 완성된 코드를 한 번에 붙여넣었다면, 아래 순서대로 **주석을 지웠다 붙였다** 하며
> 단계별로 커밋해도 되고, 실제로 기능을 하나씩 만들어가며 커밋해도 좋습니다.

### main 브랜치에서 진행 (1~5번)

```bash
# 1) 프로젝트 초기화
git add .gitignore
git commit -m "chore: 프로젝트 초기화 및 .gitignore 추가"

# 2) README 초안
git add README.md
git commit -m "docs: README 초안 작성"

# 3) 메뉴 및 메인 루프 골격
git add prompt_manager.py
git commit -m "feat: 메뉴 출력 및 메인 루프 골격 작성"

# 4) 기본 프롬프트 JSON 저장/불러오기 기능
git add prompt_manager.py
git commit -m "feat: 기본 프롬프트 JSON 저장/불러오기 기능 추가"

# 5) 프롬프트 추가 기능
git add prompt_manager.py
git commit -m "feat: 프롬프트 추가 기능 구현"

git push
```

### 브랜치에서 진행 (6번 - 프롬프트 목록 기능)

요구사항에 따라 **프롬프트 목록 기능은 반드시 별도 브랜치에서 작업 후 병합**합니다.

```bash
# 새 브랜치 생성 및 이동
git checkout -b feature/show-list

# show_all_prompts() 기능 작성 후
git add prompt_manager.py
git commit -m "feat: 프롬프트 목록 조회 기능 구현"

# (원한다면 브랜치에서 원격으로도 올려보기)
git push -u origin feature/show-list

# 작업이 끝나면 main으로 돌아가 병합
git checkout main
git merge feature/show-list

# 병합 완료 후 브랜치 삭제 (선택)
git branch -d feature/show-list

git push
```

### 다시 main 브랜치에서 진행 (7~15번)

```bash
# 7) 검색 기능
git add prompt_manager.py
git commit -m "feat: 제목/내용 검색 기능 구현"

# 8) 상세 보기 기능
git add prompt_manager.py
git commit -m "feat: 프롬프트 상세 보기 기능 구현"

# 9) 카테고리별 조회 기능
git add prompt_manager.py
git commit -m "feat: 카테고리별 조회 기능 구현"

# 10) 즐겨찾기 추가/해제 기능
git add prompt_manager.py
git commit -m "feat: 즐겨찾기 추가/해제 기능 구현"

# 11) 즐겨찾기 목록 기능
git add prompt_manager.py
git commit -m "feat: 즐겨찾기 목록 기능 구현"

# 12) 프롬프트 수정/삭제 기능
git add prompt_manager.py
git commit -m "feat: 프롬프트 수정, 삭제 기능 추가"

# 13) 예외 처리 보완
git add prompt_manager.py
git commit -m "fix: 잘못된 입력값 처리 보완"

# 14) README 보강
git add README.md
git commit -m "docs: README에 실행 방법 및 기능 목록 보강"

# 15) 최종 정리
git add .
git commit -m "chore: 코드 정리 및 주석 보완"

git push
```

### 마지막으로 로그 확인

```bash
git log --oneline --graph
```

브랜치가 생성되었다가 `main`으로 합쳐진 지점이 그래프로 보이면 성공입니다.
이 화면을 캡처해서 제출하면 됩니다.

---

| 브랜치 진행 로그 | 브랜치 그래픽화 |
|---|---|
|<img width="695" height="424" alt="image" src="https://github.com/user-attachments/assets/3671b4aa-a79e-49c5-baad-cecda4fb0c72" />|<img width="657" height="564" alt="image" src="https://github.com/user-attachments/assets/ef671286-57b4-4ef1-aa59-4d381023e5bc" />|


작업은 항상 main 브랜치에서 진행되었습니다.
원격 저장소(origin/main)의 변경 사항을 여러 번 병합하면서 최신 상태를 유지했습니다.
새로운 브랜치를 만들어서 작업한 흔적은 없고, main 브랜치에서 직접 커밋과 병합을 반복한 구조입니다.


## Git 명령어 한눈에 정리

| 명령어 | 의미 |
|---|---|
| `git init` | 현재 폴더를 Git 저장소로 초기화 |
| `git add <파일>` | 변경 내용을 커밋 대상(스테이징 영역)에 추가 |
| `git commit -m "메시지"` | 스테이징된 변경 내용을 하나의 기록으로 저장 |
| `git push` | 로컬 커밋을 원격 저장소(GitHub)로 업로드 |
| `git pull` | 원격 저장소의 최신 내용을 로컬로 가져오기 |
| `git checkout -b <브랜치명>` | 새 브랜치를 만들고 그 브랜치로 이동 |
| `git checkout <브랜치명>` | 다른 브랜치로 이동 |
| `git merge <브랜치명>` | 지정한 브랜치의 변경 내용을 현재 브랜치로 합치기 |
| `git clone <URL>` | 원격 저장소를 통째로 로컬에 복사해오기 |
| `git log --oneline --graph` | 커밋 기록을 그래프 형태로 한 줄씩 보기 |

---

## 학습 목표 자가 점검

이 과제를 마친 후 아래 항목을 스스로 설명할 수 있는지 점검해보세요.

- [ ] VSCode에서 Python 파일을 생성하고 실행할 수 있다.
- [ ] 터미널에서 Python/Git 버전을 확인하고 필요한 설정을 점검할 수 있다.
- [ ] 파이썬 기초 문법(변수, 리스트, 딕셔너리, 반복문, 함수 등)을 설명할 수 있다.
- [ ] Git이 무엇이고 왜 필요한지 설명할 수 있다.
- [ ] `init`, `add`, `commit`, `push`, `pull`, `checkout`, `clone`, `merge`가
      각각 무엇을 하는지 설명할 수 있다.
- [ ] 브랜치를 생성하고 병합할 수 있다.
- [ ] GitHub에 코드를 업로드하고 관리할 수 있다.

---

## Git이란?

Git은 소스 코드의 변경 이력을 관리하는 **분산 버전 관리 시스템(DVCS, Distributed Version Control System)**이다.
파일이 어떻게 변경되어 왔는지를 시간 순서대로 기록하고, 필요하면 과거 특정 시점으로 되돌리거나 여러 사람이 동시에 작업한 내용을 하나로 합칠 수 있게 해준다.

### Git의 핵심 특징

- **분산 구조**: 중앙 서버(예: GitHub)뿐 아니라 개발자 각자의 로컬 컴퓨터에도 전체 저장소(히스토리 포함)의 복사본이 존재한다. 서버가 다운되어도 로컬에서 작업을 계속할 수 있다.
- **스냅샷 기반 저장**: 파일의 "변경분(diff)"만 저장하는 다른 시스템과 달리, Git은 커밋할 때마다 전체 프로젝트 상태를 스냅샷으로 저장한다. (내부적으로는 중복을 제거해 효율적으로 저장)
- **빠르고 가벼운 브랜치**: 브랜치 생성/전환 비용이 매우 낮아 새로운 기능이나 실험을 부담 없이 시도할 수 있다.
- **무결성 보장**: 모든 커밋은 SHA-1 해시로 식별되어, 파일이나 히스토리가 변조되면 즉시 감지할 수 있다.
- **비선형 개발 지원**: 여러 브랜치에서 동시에 작업하고 나중에 병합(merge)하는 방식의 개발이 가능하다.

### Git이 왜 필요한가

1. **변경 이력 관리**: "언제, 누가, 무엇을, 왜" 변경했는지 커밋 메시지와 함께 기록되므로 문제가 생겼을 때 원인 추적이 쉽다.
2. **협업 지원**: 여러 명이 같은 프로젝트를 동시에 작업해도 각자의 변경 사항을 브랜치로 분리했다가 병합할 수 있어 충돌을 최소화한다.
3. **백업 및 복구**: 실수로 코드를 삭제하거나 잘못 수정해도 이전 커밋으로 되돌릴 수 있다.
4. **실험적 개발 가능**: 새로운 기능을 메인 코드에 영향을 주지 않고 별도 브랜치에서 자유롭게 시도해볼 수 있다.
5. **코드 리뷰 및 품질 관리**: GitHub 등과 연동하면 Pull Request를 통해 다른 사람의 코드를 리뷰하고 병합 전에 품질을 검증할 수 있다.
6. **원격 협업**: GitHub, GitLab 같은 원격 저장소를 통해 지리적으로 떨어진 팀원들과도 코드를 공유하고 동기화(push/pull)할 수 있다.

### Git이 없다면?

- 파일을 `최종.py`, `최종_진짜.py`, `최종_진짜_수정.py` 처럼 수동으로 버전 관리해야 한다.
- 여러 명이 같은 파일을 수정하면 서로의 작업을 덮어쓸 위험이 크다.
- 특정 시점의 코드로 되돌리기 어렵고, 무엇이 왜 바뀌었는지 알기 힘들다.



## 브랜치 전략 비교

| 구분 | Git Flow | GitHub Flow | GitLab Flow | Trunk-Based Development |
|---|---|---|---|---|
| **핵심 개념** | 기능/릴리즈/핫픽스를 위한 다수의 정형화된 브랜치 체계 | 단일 `main` 브랜치 + 기능 브랜치의 단순한 흐름 | GitHub Flow에 환경별(스테이징/프로덕션) 브랜치 개념을 추가 | 모든 개발자가 하나의 브랜치(trunk/main)에 매우 자주 병합 |
| **주요 브랜치** | `main`, `develop`, `feature/*`, `release/*`, `hotfix/*` | `main`, `feature/*` | `main`, `pre-production`, `production`, `feature/*` (또는 환경별 브랜치) | `main` (trunk) + 수명이 짧은 feature 브랜치 |
| **브랜치 생명주기** | 길다 (release, develop은 상시 유지) | 짧다 (기능 완료 후 바로 병합·삭제) | 중간 (환경 브랜치는 상시, feature는 짧게) | 매우 짧다 (하루~며칠 내 병합) |
| **릴리즈 방식** | 정해진 주기로 `release` 브랜치를 따서 QA 후 배포 | `main`에 병합되면 바로/지속적으로 배포 (CD 지향) | 환경 브랜치를 따라 순차적으로 배포 | `main`에서 언제든 배포 가능한 상태 유지, 배포는 별도 태그/플래그로 제어 |
| **병합 빈도** | 낮음 (기능 단위로 크게 병합) | 높음 | 중간~높음 | 매우 높음 (하루 여러 번) |
| **CI/CD 적합성** | 상대적으로 낮음 (브랜치가 오래 살아있어 통합 지연) | 높음 | 높음 | 매우 높음 (지속적 통합의 이상적 형태) |
| **충돌(Merge Conflict) 위험** | 브랜치 수명이 길어 충돌 위험 큼 | 낮음 (자주 병합) | 낮음~중간 | 매우 낮음 (항상 최신 상태 유지) |
| **학습 난이도** | 높음 (브랜치 종류와 규칙이 많음) | 낮음 (구조가 단순) | 중간 | 낮음 (구조는 단순하나 팀 규율 필요) |
| **장점** | 릴리즈/핫픽스 관리가 체계적, 대규모·정기 릴리즈에 적합 | 단순하고 이해하기 쉬움, 빠른 배포에 적합 | 환경별 배포 이력 관리가 명확함 | 통합 지연 없음, 충돌 최소화, CI/CD와 궁합이 좋음 |
| **단점** | 브랜치가 많아 복잡함, 병합 지연으로 충돌 발생 가능 | 버전 관리(릴리즈 태깅)가 약함, 여러 배포 환경 관리에 불리 | Git Flow보다는 단순하지만 여전히 규칙이 필요함 | 팀의 높은 규율과 강력한 테스트 자동화 필요, 기능 플래그 관리 부담 |
| **적합한 상황** | 정해진 일정으로 배포하는 대규모 소프트웨어, 여러 버전을 동시에 유지보수해야 하는 경우 | 웹 서비스 등 지속적 배포(CD)를 하는 소규모~중규모 팀 | 여러 배포 환경(스테이징/프로덕션)을 명확히 구분해야 하는 팀 | 빠른 반복과 지속적 배포를 지향하는 팀, 성숙한 테스트 문화를 가진 팀 |
| **대표 사용 예** | 데스크톱/모바일 앱, 정기 버전 출시 소프트웨어 | GitHub 자체, 다수의 오픈소스 웹 프로젝트 | GitLab, 다단계 배포 파이프라인을 쓰는 조직 | Google, Facebook 등 대규모 CI/CD 조직 |

### 이번 과제(프롬프트 관리 프로그램)에 적용한다면?

- 이번 과제처럼 **혼자서, 단기간에, 기능 단위로 브랜치를 만들고 병합**하는 경우는 복잡한 Git Flow보다 **GitHub Flow**에 가까운 간단한 전략이 적합하다.
- 예: `main`에서 `feature/show-list` 같은 기능 브랜치를 만들어 작업 → 완료 후 `main`으로 `checkout` + `merge` → 다음 기능 브랜치 생성, 반복.



## 제출물 체크리스트

- [ ] GitHub 저장소 URL
- [ ] 개발 환경 설정 스크린샷 (VSCode, `python --version`, `git config --list` 등)
- [ ] 프로그램 실행 결과 스크린샷 (메뉴, 프롬프트 추가, 목록, 검색 등)
- [ ] `git log --oneline --graph` 결과 스크린샷 (브랜치 병합 지점 포함)

> 위 스크린샷들은 실제로 실습을 진행한 화면으로 교체해서 제출하시면 됩니다.
