1. 필요한 API

이 프로젝트는 **텍스트 생성(LLM)**과 이미지 생성, 두 가지 종류의 API가 필요합니다. 이 둘은 보통 하나의 회사에서 동시에 제공하지 않기 때문에 조합을 정해야 합니다.

용도	옵션	비고
텍스트 생성 (네이밍/슬로건/스토리/컬러)	Anthropic Claude API	코드/설명 생성 품질이 좋고, 한국어 자연스러움도 우수. 이미지 생성은 지원 안 함
텍스트 생성 (대안)	OpenAI GPT API	마찬가지로 이미지 생성은 별도(DALL-E)
로고 이미지 생성	OpenAI DALL-E API	요구사항 문서에 명시된 "DALL-E 등"에 해당. 현재 가장 접근성 좋은 이미지 생성 API
로고 이미지 생성 (대안)	Stability AI (Stable Diffusion) API	DALL-E 대안, 별도 가입 필요

즉 가장 표준적인 조합은:

텍스트: Claude API 또는 OpenAI API
이미지: OpenAI DALL-E API (이미지는 Claude API로 불가능)


2. 개발 환경
항목	내용
Python	3.9 이상 (권장 3.11+)
패키지 관리	venv (가상환경) + pip
필수 라이브러리	anthropic 또는 openai(텍스트), openai(이미지, DALL-E 사용 시), python-dotenv(API 키 관리), matplotlib(컬러 팔레트 시각화), Pillow(이미지 저장/처리), requests
API 키 저장	.env 파일 (코드에 하드코딩 금지 — 요구사항 10번)
버전 관리	Git + GitHub 계정

OpenAI GPT + DALL·E API 기반으로 전체 프로젝트를 완성했습니다.

구조 요약

main.py — CLI 진입점 (입력 → 브리프 로딩 → 네이밍/슬로건/스토리/컬러 생성 → 컬러 팔레트 시각화 → 로고 생성 → JSON 저장)
modules/config.py — .env에서 OPENAI_API_KEY 로딩, 키 누락 시 안내 후 종료
modules/brief_loader.py — 브리프 JSON 검증
modules/text_generator.py — GPT API로 네이밍/슬로건/스토리/컬러(HEX) 생성, 각 단계 실패해도 다음 단계 계속 진행
modules/image_generator.py — DALL·E 3로 로고 시안 2~3개 생성/저장
modules/palette_visualizer.py — matplotlib로 컬러 팔레트 PNG 생성 (한글 폰트 자동 적용 — 실제 테스트해서 한글 깨짐 없는 것 확인함)
modules/result_saver.py — brand_result.json 저장
README.md — 프로젝트 소개/과제목표/기능요구사항(전체 표 정리) + 에러 처리 표(과제목표 4번용) + 초보자용 Python/venv/Git 설정 가이드


바로 실행 전 필요한 준비물

Python 3.9+ 설치
pip install -r requirements.txt
OpenAI API 키 발급 → .env 파일에 OPENAI_API_KEY=sk-... 입력 (.env.example 참고)

python3 main.py 실행 후 sample_brief.json을 브리프 파일로 입력하면 바로 테스트해볼 수 있습니다. 코드 문법 검사와 컬러 팔레트 시각화 기능은 이미 실행 테스트를 마쳤고(위 이미지처럼 정상 동작), 실제 GPT/DALL·E 호출 부분은 사용자님의 API 키로 실행하셔야 확인 가능합니다.
